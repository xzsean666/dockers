version: "3.8"

services:
  redis:
    image: redis:8.2
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    # command: ["redis-server", "--requirepass", "666.", "--appendonly", "yes"]
