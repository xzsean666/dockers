version: '3'
services:
  frps:
    image: snowdreamtech/frps
    container_name: frps
    restart: always
    network_mode: host
    volumes:
      - ./frps.ini:/etc/frp/frps.ini
    env_file:
      - .env